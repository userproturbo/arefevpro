export type StationMode = "idle" | "projects" | "photo" | "video" | "audio" | "blog";

export const STATION_MODES: StationMode[] = [
  "idle",
  "projects",
  "photo",
  "video",
  "audio",
  "blog",
];
